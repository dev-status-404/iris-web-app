"use client";

import SettingTabs from "./ui/setting-tab";

const SettingsLayout = () => {
  return (
    <div style={{ padding: "32px 40px" }}>
      <SettingTabs />
    </div>
  );
};

export default SettingsLayout;
