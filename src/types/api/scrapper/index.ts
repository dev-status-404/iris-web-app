export type ScrapeInstagramInput = {
  profileUrl: string;
  user_id: string;
  folder_id?: string;
};

export type ScrapeLinkedinInput = {
  profile_url: string;
  user_id: string;
  folder_id?: string;
};